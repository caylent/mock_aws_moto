#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name='mock_aws',
    version="0.1.0",
    author='Michael Camilo',
    url='https://github.com/michael-caylent/mock_aws',
    scripts=[],
    packages=find_packages(),
    install_requires=[]
)
